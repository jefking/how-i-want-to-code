services:
  harness:
    build:
      context: .
      dockerfile: Dockerfile
    image: moltenhub-code:local
    ports:
      - 3232:7777
    shm_size: "8gb"
    volumes:
      - ./.moltenhub/config:/workspace/config
    command: ["with-config"]
