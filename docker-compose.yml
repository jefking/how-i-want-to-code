services:
  harness:
    image: moltenhub-code:latest
    build:
      context: .
    ports:
      - 3232:7777
    env_file:
      - .env
    volumes:
      - ./docker/config:/.moltenhub/
    command: ["with-config"]
