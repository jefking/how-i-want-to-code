services:
  harness:
    image: moltenhub-code:vnext
    pull_policy: build
    build:
      context: .
    ports:
      - 3232:7777
    env_file:
      - .env
    volumes:
      - ./docker/config:/.moltenhub
    command: ["/usr/local/bin/with-config"]
